<craft>
    <parameter name="id" type="int" default="935536"/>
    <script type="text/craftml">        

		var thing = require('irjs-thingiverse')

      function main(params){
         console.log('downloading thing id = %d', params.id)

			var flag = function(req, res, cb){

				if(flag != err)
					var file = 'node_modules/irjs-thingiverse/files/' +params.id + '.stl'
            return '<stl src="./' + file + '"/>'
        }

		}

		function cb(){
			thing.getFileById(params.id)
		}
    </script>
</craft>
